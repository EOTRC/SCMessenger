# Security Rules -- moved to `docs/rules/SECURITY_PROTOCOL.md`

Stub only. `core/src/{crypto,transport,routing,privacy}/` changes are
merge-blocked until adversarial review signs off -- read the full protocol first.
